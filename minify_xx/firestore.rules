rules_version = '2';

// REDEPLOYED BY AGENT TO ENSURE PERMISSIVE RULES ARE ACTIVE
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
