import native from"./native.js";import{defaultHashLength}from"../base/hash-fn.js";export const normalizeInput=(e,t)=>e instanceof Buffer?e:"string"==typeof e?Buffer.from(e,t):Buffer.from(e);export function hash(e,{length:t=defaultHashLength}={}){return native.hash(normalizeInput(e),t)}export function deriveKey(e,t,{length:n=defaultHashLength}={}){const a=new native.Hasher(void 0,e);a.update(normalizeInput(t));const r=Buffer.alloc(n);return a.digest(r),r}export function keyedHash(e,t,{length:n=defaultHashLength}={}){const a=new native.Hasher(e);a.update(normalizeInput(t));const r=Buffer.alloc(n);return a.digest(r),r}