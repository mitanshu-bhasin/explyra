const memoizer=require("./.."),assert=require("chai").assert;describe("lru-memoizer sync (freeze)",()=>{describe("call",()=>{let e,r=0;beforeEach(()=>{r=0,e=memoizer.sync({load:e=>(r++,{foo:e,buffer:Buffer.from("1234")}),hash:e=>e,freeze:!0})}),it("should return a freeze every time with the same cached structure",()=>{const s=e("bar");assert.strictEqual(r,1),assert.equal(s.foo,"bar"),assert.isFrozen(s);const a=e("bar");assert.strictEqual(r,1),assert.equal(a.foo,"bar"),assert.isFrozen(a)})}),describe("Promise",()=>{let e,r=0;beforeEach(()=>{r=0,e=memoizer.sync({load:e=>(r++,Promise.resolve({foo:e,buffer:Buffer.from("1234")})),hash:e=>e,freeze:!0})}),it("should return a freeze every time with the same cached structure",s=>{e("bar").then(a=>{assert.strictEqual(r,1),assert.equal(a.foo,"bar"),assert.isFrozen(a),e("bar").then(e=>{assert.strictEqual(r,1),assert.equal(e.foo,"bar"),assert.isFrozen(e),s()})}).catch(s)})})});