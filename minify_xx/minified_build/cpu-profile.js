"use strict";const privateCpuProfileName=process.env.__NEXT_PRIVATE_CPU_PROFILE,isCpuProfileEnabled=process.env.NEXT_CPU_PROF||privateCpuProfileName;if(isCpuProfileEnabled){const{Session:e}=require("inspector"),r=require("fs"),o=new e;function i(){o.post("Profiler.stop",(e,o)=>{if(e)return void console.error("Cannot generate CPU profiling:",e);const i=`${privateCpuProfileName||"CPU.main"}.${Date.now()}.cpuprofile`;r.writeFileSync(`./${i}`,JSON.stringify(o.profile)),process.exit(0)})}o.connect(),o.post("Profiler.enable"),o.post("Profiler.start"),process.on("SIGINT",i),process.on("SIGTERM",i),process.on("exit",i)}