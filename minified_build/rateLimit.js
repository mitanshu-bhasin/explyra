const logger=require("debug")("jwks"),{RateLimiter:RateLimiter}=require("limiter"),JwksRateLimitError=require("../errors/JwksRateLimitError");function rateLimitWrapper(e,{jwksRequestsPerMinute:t=10}){const r=e.getSigningKey.bind(e),i=new RateLimiter(t,"minute",!0);return logger(`Configured rate limiting to JWKS endpoint at ${t}/minute`),async e=>await new Promise((t,n)=>{i.removeTokens(1,async(i,o)=>{if(i&&n(i),logger("Requests to the JWKS endpoint available for the next minute:",o),o<0)logger("Too many requests to the JWKS endpoint"),n(new JwksRateLimitError("Too many requests to the JWKS endpoint"));else try{const i=await r(e);t(i)}catch(e){n(e)}})})}module.exports.default=rateLimitWrapper;