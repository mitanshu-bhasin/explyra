import fs from"fs";import path from"path";import{imageExtMimeTypeMap}from"../../../lib/mime-type";import{getLoaderModuleNamedExports}from"./utils";import{installBindings}from"../../swc/install-bindings";function errorOnBadHandler(e){return`\n  if (typeof handler !== 'function') {\n    throw new Error('Default export is missing in ${JSON.stringify(e)}')\n  }\n  `}async function createReExportsCode(e,n){const t=(await getLoaderModuleNamedExports(e,n)).filter(e=>"default"!==e&&"generateSitemaps"!==e&&"dynamicParams"!==e);return t.length>0?`export { ${t.join(", ")} } from ${JSON.stringify(e)}\n`:""}const CACHE_HEADERS={NO_CACHE:"no-cache, no-store",REVALIDATE:"public, max-age=0, must-revalidate"};export function getFilenameAndExtension(e){const n=path.basename(e),[t,a]=n.split(".",2);return{name:t,ext:a}}function getContentType(e){let{name:n,ext:t}=getFilenameAndExtension(e);return"jpg"===t&&(t="jpeg"),"favicon"===n&&"ico"===t?"image/x-icon":"sitemap"===n?"application/xml":"robots"===n?"text/plain":"manifest"===n?"application/manifest+json":"png"===t||"jpeg"===t||"ico"===t||"svg"===t?imageExtMimeTypeMap[t]:"text/plain"}async function getStaticAssetRouteCode(e,n){const t="production"!==process.env.NODE_ENV?CACHE_HEADERS.NO_CACHE:CACHE_HEADERS.REVALIDATE,a="twitter-image"===n,r="opengraph-image"===n,i=a?5:8,o=a?"Twitter":"Open Graph";return`/* static asset route */\nimport { NextResponse } from 'next/server'\n\nconst contentType = ${JSON.stringify(getContentType(e))}\nconst buffer = Buffer.from(${JSON.stringify((await fs.promises.readFile(e)).toString("base64"))}, 'base64'\n  )\n\nif (${a||r}) {\n  const fileSizeInMB = buffer.byteLength / 1024 / 1024\n  if (fileSizeInMB > ${i}) {\n    throw new Error('File size for ${o} image ${JSON.stringify(e)} exceeds ${i}MB. ' +\n    \`(Current: \${fileSizeInMB.toFixed(2)}MB)\n\` +\n    'Read more: https://nextjs.org/docs/app/api-reference/file-conventions/metadata/opengraph-image#image-files-jpg-png-gif'\n    )\n  }\n}\n\nexport function GET() {\n  return new NextResponse(buffer, {\n    headers: {\n      'Content-Type': contentType,\n      'Cache-Control': ${JSON.stringify(t)},\n    },\n  })\n}\n\nexport const dynamic = 'force-static'\n`}async function getDynamicTextRouteCode(e,n){return`/* dynamic asset route */\nimport { NextResponse } from 'next/server'\nimport handler from ${JSON.stringify(e)}\nimport { resolveRouteData } from 'next/dist/build/webpack/loaders/metadata/resolve-route-data'\n\nconst contentType = ${JSON.stringify(getContentType(e))}\nconst fileType = ${JSON.stringify(getFilenameAndExtension(e).name)}\n\n${errorOnBadHandler(e)}\n${await createReExportsCode(e,n)}\n\nexport async function GET() {\n  const data = await handler()\n  const content = resolveRouteData(data, fileType)\n\n  return new NextResponse(content, {\n    headers: {\n      'Content-Type': contentType,\n      'Cache-Control': ${JSON.stringify(CACHE_HEADERS.REVALIDATE)},\n    },\n  })\n}\n`}async function getDynamicImageRouteCode(e,n){return`/* dynamic image route with generateImageMetadata */\nimport { NextResponse } from 'next/server'\nimport { default as handler, generateImageMetadata } from ${JSON.stringify(e)}\n\n${errorOnBadHandler(e)}\n${await createReExportsCode(e,n)}\n\nexport async function GET(_, ctx) {\n  const paramsPromise = ctx.params\n  const idPromise = paramsPromise.then(params => params?.__metadata_id__)\n  const restParamsPromise = paramsPromise.then(params => {\n    if (!params) return undefined\n    const { __metadata_id__, ...rest } = params\n    return rest\n  })\n\n  const restParams = await restParamsPromise\n  const __metadata_id__ = await idPromise\n  const imageMetadata = await generateImageMetadata({ params: restParams })\n  const id = imageMetadata.find((item) => {\n    if (item?.id == null) {\n      throw new Error('id property is required for every item returned from generateImageMetadata')\n    }\n\n    return item.id.toString() === __metadata_id__\n  })?.id\n\n  if (id == null) {\n    return new NextResponse('Not Found', {\n      status: 404,\n    })\n  }\n\n  return handler({ params: restParamsPromise, id: idPromise })\n}\n\nexport async function generateStaticParams({ params }) {\n  const imageMetadata = await generateImageMetadata({ params })\n  const staticParams = []\n\n  for (const item of imageMetadata) {\n    if (item?.id == null) {\n      throw new Error('id property is required for every item returned from generateImageMetadata')\n    }\n    staticParams.push({ __metadata_id__: item.id.toString() })\n  }\n  return staticParams\n}\n`}async function getSingleImageRouteCode(e,n){return`/* dynamic image route without generateImageMetadata */\nimport { NextResponse } from 'next/server'\nimport { default as handler } from ${JSON.stringify(e)}\n\n${errorOnBadHandler(e)}\n${await createReExportsCode(e,n)}\n\nexport async function GET(_, ctx) {\n  return handler({ params: ctx.params })\n}\n`}async function getImageRouteCode(e,n){return(await getLoaderModuleNamedExports(e,n)).includes("generateImageMetadata")?getDynamicImageRouteCode(e,n):getSingleImageRouteCode(e,n)}async function getSingleSitemapRouteCode(e,n){return`/* single sitemap route */\nimport { NextResponse } from 'next/server'\nimport { default as handler } from ${JSON.stringify(e)}\nimport { resolveRouteData } from 'next/dist/build/webpack/loaders/metadata/resolve-route-data'\n\nconst contentType = ${JSON.stringify(getContentType(e))}\nconst fileType = ${JSON.stringify(getFilenameAndExtension(e).name)}\n\n${errorOnBadHandler(e)}\n${await createReExportsCode(e,n)}\n\nexport async function GET() {\n  const data = await handler()\n  const content = resolveRouteData(data, fileType)\n\n  return new NextResponse(content, {\n    headers: {\n      'Content-Type': contentType,\n      'Cache-Control': ${JSON.stringify(CACHE_HEADERS.REVALIDATE)},\n    },\n  })\n}\n`}async function getDynamicSitemapRouteCode(e,n){return`/* dynamic sitemap route with generateSitemaps */\nimport { NextResponse } from 'next/server'\nimport { default as handler, generateSitemaps } from ${JSON.stringify(e)}\nimport { resolveRouteData } from 'next/dist/build/webpack/loaders/metadata/resolve-route-data'\n\nconst contentType = ${JSON.stringify(getContentType(e))}\nconst fileType = ${JSON.stringify(getFilenameAndExtension(e).name)}\n\n${errorOnBadHandler(e)}\n${await createReExportsCode(e,n)}\n\nexport async function GET(_, ctx) {\n  const paramsPromise = ctx.params\n  const idPromise = paramsPromise.then(params => params?.__metadata_id__)\n\n  const id = await idPromise\n  const hasXmlExtension = id ? id.endsWith('.xml') : false\n  const sitemaps = await generateSitemaps()\n  let foundId\n  for (const item of sitemaps) {\n    if (item?.id == null) {\n      throw new Error('id property is required for every item returned from generateSitemaps')\n    }\n\n    const baseId = id && hasXmlExtension ? id.slice(0, -4) : undefined\n    if (item.id.toString() === baseId) {\n      foundId = item.id\n    }\n  }\n  if (foundId == null) {\n    return new NextResponse('Not Found', {\n      status: 404,\n    })\n  }\n\n  const targetIdPromise = idPromise.then(id => {\n    const hasXmlExtension = id ? id.endsWith('.xml') : false\n    return id && hasXmlExtension ? id.slice(0, -4) : undefined\n  })\n  const data = await handler({ id: targetIdPromise })\n  const content = resolveRouteData(data, fileType)\n\n  return new NextResponse(content, {\n    headers: {\n      'Content-Type': contentType,\n      'Cache-Control': ${JSON.stringify(CACHE_HEADERS.REVALIDATE)},\n    },\n  })\n}\n\nexport async function generateStaticParams() {\n  const sitemaps = await generateSitemaps()\n  const params = []\n\n  for (const item of sitemaps) {\n    if (item?.id == null) {\n      throw new Error('id property is required for every item returned from generateSitemaps')\n    }\n    params.push({ __metadata_id__: item.id.toString() + '.xml' })\n  }\n  return params\n}\n`}async function getSitemapRouteCode(e,n){return(await getLoaderModuleNamedExports(e,n)).includes("generateSitemaps")?getDynamicSitemapRouteCode(e,n):getSingleSitemapRouteCode(e,n)}const nextMetadataRouterLoader=async function(){await installBindings();const{isDynamicRouteExtension:e,filePath:n}=this.getOptions(),{name:t}=getFilenameAndExtension(n);this.addDependency(n);let a="";return a="1"===e?"robots"===t||"manifest"===t?await getDynamicTextRouteCode(n,this):"sitemap"===t?await getSitemapRouteCode(n,this):await getImageRouteCode(n,this):await getStaticAssetRouteCode(n,t),a};export default nextMetadataRouterLoader;