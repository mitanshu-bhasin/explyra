document.addEventListener("DOMContentLoaded",()=>{initStatusPage()});async function initStatusPage(){await refreshStatus();setInterval(refreshStatus,6e4)}async function refreshStatus(){try{const response=await fetch("data/services.json?t="+Date.now());const data=await response.json();renderOverallStatus(data.overall_status);renderServices(data.groups);renderIncidents(data.incidents)}catch(error){console.error("Error fetching status data:",error)}}function renderOverallStatus(status){const banner=document.getElementById("overall-status");if(!banner)return;banner.className="status-banner "+status;const statusMap={operational:"🟢 All Systems Operational",disruption:"🟡 Partial Service Disruption",outage:"🔴 Major Service Outage",maintenance:"🔵 Scheduled Maintenance"};banner.innerText=statusMap[status]||"🟢 All Systems Operational"}function renderServices(groups){const coreContainer=document.getElementById("core-services");const productContainer=document.getElementById("product-systems");const infraContainer=document.getElementById("infra-services");if(!coreContainer||!productContainer||!infraContainer)return;coreContainer.innerHTML="";productContainer.innerHTML="";infraContainer.innerHTML="";groups.forEach(group=>{let container;if(group.name.includes("Core"))container=coreContainer;else if(group.name.includes("Product"))container=productContainer;else if(group.name.includes("Infrastructure"))container=infraContainer;if(container){group.services.forEach(service=>{const card=document.createElement("status-card");card.setAttribute("name",service.name);card.setAttribute("status",service.status);card.setAttribute("last-checked",new Date(service.last_checked).toLocaleString());container.appendChild(card)})}})}function renderIncidents(incidents){const container=document.getElementById("incident-history");if(!container)return;container.innerHTML="";incidents.forEach(incident=>{const item=document.createElement("div");item.className="incident-item";item.innerHTML=`\n            <div class="incident-date">${incident.date}</div>\n            <div class="incident-title">${incident.title}</div>\n            <div class="incident-status">${incident.status}</div>\n        `;container.appendChild(item)})}