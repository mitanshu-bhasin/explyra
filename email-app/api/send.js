import{Resend}from"resend";import{auth}from"./_lib/firebase.js";const resend=new Resend(process.env.RESEND_API_KEY||"re_frUajX2k_7oWbz3faKEkzYcH4hfJetQnw");export default async function handler(req,res){if(req.method==="OPTIONS")return res.status(200).end();if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});const token=req.headers.authorization?.split("Bearer ")[1];if(!token)return res.status(401).json({error:"No auth token"});try{const decoded=await auth.verifyIdToken(token);const{to:to,cc:cc,bcc:bcc,subject:subject,htmlBody:htmlBody,textBody:textBody,originalMessageId:originalMessageId}=req.body;if(!to||!subject){return res.status(400).json({error:"Missing required fields: to, subject"})}const fromAddress=`${decoded.name||"User"} <${decoded.email.split("@")[0]}@${process.env.EMAIL_DOMAIN||"explyra.me"}>`;const plainText=(textBody||"").trim();const richHtml=(htmlBody||"").trim();const emailOptions={from:fromAddress,to:[to],subject:subject,html:richHtml||`<p>${plainText}</p>`,text:plainText||richHtml.replace(/<[^>]+>/g,"")||subject};if(cc)emailOptions.cc=cc.split(",").map(e=>e.trim());if(bcc)emailOptions.bcc=bcc.split(",").map(e=>e.trim());if(originalMessageId){emailOptions.headers={"In-Reply-To":originalMessageId,References:originalMessageId}}const{data:data,error:error}=await resend.emails.send(emailOptions);if(error){console.error("Resend API Error:",error);return res.status(error.statusCode||400).json({error:error.message,details:error})}return res.status(200).json({success:true,id:data.id})}catch(error){console.error("Error sending email:",error);return res.status(500).json({error:"Failed to send email"})}}