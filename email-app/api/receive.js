import{db}from"./_lib/firebase.js";export default async function handler(req,res){if(req.method==="OPTIONS")return res.status(200).end();if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});const secret=req.headers["x-webhook-secret"];if(secret!==process.env.WEBHOOK_SECRET){return res.status(401).json({error:"Unauthorized"})}try{const{messageId:messageId,fromName:fromName,fromEmail:fromEmail,to:to,subject:subject,textBody:textBody,htmlBody:htmlBody}=req.body;const fromAddress=fromName?`${fromName} <${fromEmail}>`:fromEmail;let cleanText=textBody||"";let cleanHtml=htmlBody||"";if(/^(Received:|ARC-Seal:|DKIM-Signature:|MIME-Version:|From:|Message-ID:)/im.test(cleanText)){try{const{default:PostalMime}=await import("postal-mime");const parser=new PostalMime;const parsed=await parser.parse(cleanText);cleanText=parsed.text||"";cleanHtml=parsed.html||""}catch(e){console.error("PostalMime parsing failed, falling back to basic extraction:",e);const parts=cleanText.split(/\r?\n\r?\n/);if(parts.length>1){cleanText=parts.slice(1).join("\n\n").replace(/=\r?\n/g,"").replace(/=[0-9A-Fa-f]{2}/g,m=>String.fromCharCode(parseInt(m.slice(1),16))).trim()}}}await db.collection("emails").add({messageId:messageId||"",from:fromAddress||"unknown",to:to||"unknown",subject:subject||"(No Subject)",textBody:cleanText,htmlBody:cleanHtml,timestamp:(new Date).toISOString(),read:false,folder:"inbox"});return res.status(200).json({success:true})}catch(error){console.error("Error saving email:",error);return res.status(500).json({error:"Failed to save email"})}}