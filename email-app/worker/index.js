import PostalMime from"postal-mime";export default{async email(message,env,ctx){try{const rawEmail=await new Response(message.raw).arrayBuffer();const parser=new PostalMime;const parsed=await parser.parse(rawEmail);const payload={from:message.from,to:message.to,subject:parsed.subject||"(No Subject)",body:parsed.text||parsed.html||""};const response=await fetch(env.WEBHOOK_URL,{method:"POST",headers:{"Content-Type":"application/json","X-Webhook-Secret":env.WEBHOOK_SECRET},body:JSON.stringify(payload)});if(!response.ok){console.error("Webhook failed:",response.status,await response.text())}}catch(error){console.error("Email worker error:",error)}}};