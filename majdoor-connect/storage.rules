rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /aadhar/{userId}/{fileName} {
      // Only authenticated users can upload their own Aadhar
      // and only read their own
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
