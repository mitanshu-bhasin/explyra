## 2025-01-20 - Stateful Icon-Only Toggle Buttons
**Learning:** Stateful icon-only toggle buttons (like password visibility) need dynamic `aria-label` and `title` updates via JavaScript.
**Action:** Update the JS handler to dynamically reflect the current state along with initial HTML attributes.
