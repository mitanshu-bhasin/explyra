## 2024-05-24 - Interactive Element Styling Pattern
**Learning:** For interactive icon-only buttons (like password visibility toggles), always include an `aria-label` and `title` to explain their function since they lack visible text.
**Action:** When adding or updating icon-only buttons, ensure both `aria-label` and `title` are present and dynamic if the state changes.
