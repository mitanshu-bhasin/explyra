const deleteSubdomain=async subdomain=>{const user=auth.currentUser;if(!user)throw new Error("Authentication required");console.log(`[DNS] Deleting subdomain: ${subdomain}`);const subRef=db.collection("subdomains").doc(subdomain);const subDoc=await subRef.get();if(!subDoc.exists){throw new Error("Subdomain not found")}const subData=subDoc.data();if(subData.uid!==user.uid){throw new Error("Permission denied")}const dnsRecordsSnapshot=await db.collection("dns_records").where("uid","==",user.uid).where("userSubdomain","==",subdomain).get();console.log(`[DNS] Found ${dnsRecordsSnapshot.size} associated DNS records to delete.`);for(const doc of dnsRecordsSnapshot.docs){const data=doc.data();try{if(data.cloudflareId){await cloudflareService.deleteRecord(data.cloudflareId)}}catch(e){console.warn(`[DNS] Failed to delete record ${data.cloudflareId} from Cloudflare:`,e)}await doc.ref.delete()}try{if(subData.cloudflareId){await cloudflareService.deleteRecord(subData.cloudflareId)}}catch(e){console.warn(`[DNS] Failed to delete main record ${subData.cloudflareId} from Cloudflare:`,e)}await subRef.delete();const remainingSubs=await db.collection("subdomains").where("uid","==",user.uid).limit(1).get();if(remainingSubs.empty){await db.collection("users").doc(user.uid).update({hasSubdomain:false,primarySubdomain:null})}else if(subData.subdomain===(await db.collection("users").doc(user.uid).get()).data().primarySubdomain){await db.collection("users").doc(user.uid).update({primarySubdomain:remainingSubs.docs[0].id})}return true};window.deleteSubdomain=deleteSubdomain;