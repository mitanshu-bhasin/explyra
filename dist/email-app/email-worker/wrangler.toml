name = "explyra-mailbox-worker"
main = "index.js"
compatibility_date = "2026-03-01"
compatibility_flags = ["nodejs_compat"]

[vars]
FIREBASE_PROJECT_ID = "explyras"
GCP_CLIENT_EMAIL = "firebase-adminsdk-fbsvc@explyras.iam.gserviceaccount.com"

# Set these via Cloudflare Secrets:
# wrangler secret put GCP_PRIVATE_KEY

